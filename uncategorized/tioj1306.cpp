#include <iostream>
using namespace std;
const int prm = 31;
const int mod = 1000000009;
inline long long
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int tcase, n;
    cin >> tcase;
    string s, t;
    while ( tcase-- && cin >> s >> n ) {
        while ( n-- && cin >> t ) {

        }
    }


    return 0;
}
