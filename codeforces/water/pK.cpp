#include <iostream>
#include <algorithm>
using namespace std;
int a[105];
int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	int n, k, sim = 0, ans = 0, rem = 0;
	cin >> n >> k;
	for ( int i=0, x; i<n; ++i ) {
		cin >> x;
		if ( x >= 8 ) {
			k -= 8;
			ans++;
		} else {
			
		}
	}
	return 0;
}

