#include <iostream>
#include <map>
#define maxn 200005
using namespace std;
int a[maxn], dp[maxn];
map<int,int> M;
int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	int n, m, pos = -1;
	cin >> n >> m;
	for ( int i=0; i<n; ++i ) {
		cin >> a[i];
	}
	
	return 0;
}

