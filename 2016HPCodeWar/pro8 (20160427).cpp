#include<bits/stdc++.h>
using namespace std;
int main()
{
	string s;
	int n, i=0;
	while ( getline(cin, s) )
	{
		i=0;
		stringstream ss(s);
		vector<int> a, b;
		while ( ss >> n )
		{
			x=1;
			if (i)
			{
				
			}
			i++;
		}
	}
	int all=0;
	for ( int j=0; j<i; j++ )
		all += x[j];
	cout << all << endl;
	return 0;
}

